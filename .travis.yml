dist: trusty
language: java
jdk:
    - oraclejdk8
cache:
    directories:
        - $HOME/.m2

before_script:
    - cd ./mavendeps
    - mvn p2:site
    - mvn jetty:run
    - cd ..

script: mvn clean verify
